<div class="modal fade" id="deleteModal" data-backdrop="static" data-keyboard="false" tabindex="-1"
     aria-labelledby="deleteModal" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title text-danger" id="deleteModalLabel">
          <i class="fas fa-trash-alt"></i>
          Suppression
        </h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        Confirmer la suppression de <span class="item-info"></span>&nbsp;?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-edit" data-dismiss="modal">Annuler</button>
        <form action="/admin/users/delete" id="form-user-delete" method="post">
          <input type="hidden"
                 class="idValue"
                 name="id"
                 value=""
          >
          <input type="submit"
                 class="btn btn-danger"
                 form="form-user-delete"
                 value="Supprimer"
          >
        </form>
      </div>
    </div>
  </div>
</div>
